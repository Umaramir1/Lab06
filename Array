using System;

class Program
{
    static void Main()
    {
        // Declare and initialize the array of names
        string[] names = { "Abdullah", "asif", "Ali", "Aitsaam", "Umar" };

        // Print each name in the array
        foreach (string name in names)
        {
            Console.WriteLine(name);
        }
    }
}
2nd_branch
using System;
using System.Collections.Generic;

class Program
{
    static void Main()
    {
        // Create a list of 5 vegetables
        List<string> vegetables = new List<string>
        {
            "Carrot",
            "Broccoli",
            "Spinach",
            "Potato",
            "Cabbage"
        };

        // Print each vegetable
        foreach (string veg in vegetables)
        {
            Console.WriteLine(veg);
        }
    }
}

